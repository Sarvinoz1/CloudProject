---
- name: Adding DNS server ip to configuration file 
  become: true 
  become_method: sudo 
  tasks:
    - replace:
        path: /etc/resolv.conf
        regexp: '8.8.8.8'
        replace: '192.168.33.125'
        backup: yes
  